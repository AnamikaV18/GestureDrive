# Handle line endings automatically
* text=auto

# Treat all Python files as text and use Python-specific diffs
*.py text diff=python

# Ensure Windows batch files keep their specific line endings
*.bat text eol=crlf

# Treat images and compiled files as binary (don't try to "fix" them)
*.png binary
*.jpg binary
*.pyc binary
